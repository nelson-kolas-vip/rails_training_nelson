<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Reservation Notification" %></title>
    <!-- Include Bootstrap CSS for basic styling in email clients that support it -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
      /* Basic inline styles for better email client compatibility */
      body {

        line-height: 1.6;
        color: #333;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
      }
      .email-container {
        max-width: 600px;
        margin: 20px auto;
        background-color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      h1, h2, h3, h4, h5, h6 {
        color: #0056b3; /* A primary blue */
      }
      strong {
        font-weight: bold;
      }
      ul {
        list-style: none; /* Remove default list bullets */
        padding: 0;
      }
      ul li {
        margin-bottom: 8px;
      }
      /* Bootstrap classes will be applied, but some clients might strip them. */
      /* Use utility classes directly in the HTML for better compatibility. */
    </style>
  </head>

  <body>
    <div class="email-container">
      <%= yield %>
    </div>
  </body>
</html>
